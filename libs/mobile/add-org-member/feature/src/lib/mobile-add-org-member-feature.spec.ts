import { mobileAddOrgMemberFeature } from './mobile-add-org-member-feature';

describe('mobileAddOrgMemberFeature', () => {
  it('should work', () => {
    expect(mobileAddOrgMemberFeature()).toEqual(
      'mobile-add-org-member-feature'
    );
  });
});
